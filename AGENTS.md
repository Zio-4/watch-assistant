# Repository instructions

- Never use GitHub through a browser or graphical user interface unless the user explicitly requests GitHub UI use. Use the GitHub CLI or API by default.
- Watch Simulator and debug launch arguments are documented in `docs/debugging.md`.
